# Project Architecture & Context [Template]

이 파일(`architecture.md`)을 프로젝트의 `docs/` 폴더에 복사하여 사용하세요.
에이전트가 프로젝트를 처음 파악할 때 이 파일을 가장 먼저 읽도록 유도합니다.

## 1. High-Level Overview
- **Goal**: (이 프로젝트의 핵심 목표)
- **Tech Stack**:
    - Frontend:
    - Backend:
    - Database:
    - Infra:

## 2. Key Design Patterns
- (e.g., MVVM, Clean Architecture, Repository Pattern)
- (e.g., State Management Strategy)

## 3. Core Data Flow
- `[Source]` -> `[Process]` -> `[Sink]`

## 4. Directory Structure Explanation
- `/src/core`: (핵심 비즈니스 로직)
- `/src/ui`: (프레젠테이션 레이어)
